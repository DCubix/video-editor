# This file is generated by Flutter - DO NOT EDIT manually.
# Run `flutter pub get` to regenerate this file properly.

cmake_minimum_required(VERSION 3.14)

# This is a placeholder CMakeLists.txt for the flutter directory.
# The actual content will be generated when you run:
#   fvm flutter pub get
# or
#   flutter pub get

# For now, we create minimal targets to prevent CMake errors
add_library(flutter INTERFACE)
add_library(flutter_wrapper_app INTERFACE)

# Define a custom target for flutter_assemble
add_custom_target(flutter_assemble
  COMMAND ${CMAKE_COMMAND} -E echo "Warning: Run 'flutter pub get' to generate proper Flutter build files"
)

# Set placeholder variables to prevent CMake errors
set(FLUTTER_LIBRARY "" PARENT_SCOPE)
set(FLUTTER_LIBRARY_HEADERS "" PARENT_SCOPE)
set(AOT_LIBRARY "" PARENT_SCOPE)
set(PLUGIN_BUNDLED_LIBRARIES "" PARENT_SCOPE)
set(PROJECT_BUILD_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../../build" PARENT_SCOPE)
